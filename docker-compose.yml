version: "3.7"
services:
  labtecs-pe-diabetico-api:
    image: labtecs/pe-diabetico-api:${TAG}
    container_name: labtecs-pe-diabetico-api
    restart: unless-stopped
    ports:
      - 5555:5555
    env_file: .env.production
    networks:
      - labtecs
networks:
  labtecs:
    driver: bridge
